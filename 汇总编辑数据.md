# 第四章：汇总编辑数据    
1.聚合函数  
mysql里有很多内置的函数，也就有聚合函数，就是将所有数据放在一起得到一个统计值，包括但不限于max，min，count，avg，sum。  
使用的时候在函数后面加上括号，比如min（invoice_total)就是指invoice_total列里面的最小值，count（invoice_total）就是指统计invoice_total不为空的行数。   
如果想把空或者不空的内容全统计上，可以使用count（*）来统计所有行的个数。   
