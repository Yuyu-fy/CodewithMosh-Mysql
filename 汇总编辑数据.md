# 第四章：汇总编辑数据    
1.聚合函数  
mysql里有很多内置的函数，也就有聚合函数，就是将所有数据放在一起得到一个统计值，包括但不限于max，min，count，avg，sum。  
使用的时候在函数后面加上括号，比如min（invoice_total)就是指invoice_total列里面的最小值，count（invoice_total）就是指统计invoice_total不为空的行数。   
如果想把空或者不空的内容全统计上，可以使用count（*）来统计所有行的个数。   
USE sql_invoicing;
SELECT 
  'First half of 2019' AS date_range,
   SUM(invoice_total) AS total_sales,
   SUM(payment_total) AS total_payments,
   SUM(invoice_total - payment_total) AS what_we_expect
FROM invoices
WHERE invoice_date  BETWEEN '2019-01-01' AND '2019-06-30'
UNION 
SELECT 
  'Second half of 2019' AS date_range,
   SUM(invoice_total) AS total_sales,
   SUM(payment_total) AS total_payments,
   SUM(invoice_total - payment_total) AS what_we_expect
FROM invoices
WHERE invoice_date  BETWEEN '2019-07-01' AND '2019-012-30'

2.GROUP BY 语句。  
这个操作可以规定聚合函数相加的标准是什么，比如GROUP BY customer_id就是规定了相加的时候是将同一个用户的数据相加，而不是全部加在一起。这样操作可以得到比如每个顾客的消费总额这样的数据。
示例如下：USE sql_invoicing;
SELECT 
  p.date,pm.name AS payment_method,SUM(p.amount) AS total_payments
FROM payments p
JOIN payment_methods pm ON pm.payment_method_id = p.payment_method
GROUP BY date,payment_method
ORDER BY date   
最后值得注意的是GROUP BY要写在ORDER BY前面
