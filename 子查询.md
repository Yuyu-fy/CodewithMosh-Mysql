# 第五章：子查询
1.子查询。
之前的内容已经有提到过子查询，子查询就是在查询中加入一个小的查询，得到一个结果来帮助进行数据筛选。比如要选取员工中工资高于平均水平的人，示例如下：  
USE sql_hr;  
SELECT *   
FROM employees   
WHERE salary > (   
SELECT AVG(salary)   
FROM employees   
)  

2.IN运算符   
面对比如想选出用户号码不是有购买记录的人，那就需要用到IN运算符，表达在这个子查询里或者不在这个子查询里。  
示例如下：
WHERE client_id NOT IN (   
SELECT DISTINCT client_id   
FROM employees   
)     

3.ALL关键词  
ALL关键词很多时候可以和MAX函数互相替代。比如说我想找发票金额比3号用户的所有发票金额最高的都要高的人，那我的数据筛选WHERE语句有以下两种写法：  
WHERE invoice_total > ALL(SELECT invoice_total FROM customers WHERE client_id=3)  
或者  
WHERE invoice_total >(SLECT MAX(invoice_total) FROM customers WHERE client_id=3)  
两者是等价的  

4.ANY关键词  
IN 等价于 = ANY语句。
WHERE client_id = ANY (   
SELECT DISTINCT client_id   
FROM employees   
)     

5.相关子查询  
在查询问题中会出现这样一个问题，我需要的子查询是随着主查询的变化而变化的，就类似双循环中j是随着i变化的情况。这种时候我就需要用到相关子查询，示例如下：  
WHERE invoice_total >(  
SELECT AVG(invoice_total)  
FROM invoices  
WHERE client_id = i.client_id  
)  
但是这样的情况是我们的子查询也会调运主查询的表，导致调用的数据更多，程序执行时间更长。这一点需要注意。   

6.EXISTS关键词  
当我们使用IN语句时，系统执行的操作是先执行子查询，然后返回一个列表给主查询。这样的问题是，如果数据量很大，会导致子查询返回的列表很大，进而导致数据查询很慢。  
这个时候就要用到EXISTS语句，EXISTS语句就是直接让子程序判断自己的WHERE语句内容是否为TRUE然后返回给主查询，主查询会直接将TRUE的内容记录入表中，会快很多。  
示例如下：  
SELECT product_id  
FROM products p  
WHERE NOT EXISTS (  
SELECT product_id  
FROM order_items   
WHERE p.product_id =product_id  
）  
  
7.SELECT 语句中的子查询   
通常使用在如下场景，在SELECT语句中，我们后面用得到之前的SELECT的列，则会用到他，如：  
SELECT （SELECT AVG（invoice_total) AS invoice_average FROM invoices),invoice_total-(SELECT invoice_average)  
如果两个要选择的栏目都是select里子查询得到的，那最后可以这样写（SELECT invoice_total-invoice_average)  

8.FROM语句的子查询  
我们也可以在FROM使用子查询，将我们之前SELECT语句得到的虚拟表变成子查询，变成FROM对象。但是需要注意的是，一定要给子查询一个别名，这是必须的，  
如FROM （SELECT * FROM invoices ）AS total_invoice  
但是很显然，这会让我们的主查询变得非常长，所以只建议在简单查询里使用。
