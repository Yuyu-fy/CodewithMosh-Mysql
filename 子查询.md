# 第五章：子查询
1.子查询。
之前的内容已经有提到过子查询，子查询就是在查询中加入一个小的查询，得到一个结果来帮助进行数据筛选。比如要选取员工中工资高于平均水平的人，示例如下：  
USE sql_hr;  
SELECT *   
FROM employees   
WHERE salary > (   
SELECT AVG(salary)   
FROM employees   
)  

2.

 
